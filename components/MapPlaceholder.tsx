const MapPlaceholder = () => {
  return (
    <aside className="h-64 rounded-3xl bg-gray-200 text-gray-600 md:sticky md:top-24 md:h-[calc(100vh-7rem)]">
      <div className="flex h-full items-center justify-center text-lg font-semibold">Mapa</div>
    </aside>
  );
};

export const MapPlaceholderComponent = MapPlaceholder;